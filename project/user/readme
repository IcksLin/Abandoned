工程使用说明
本工程由菜单系统，线程中断，主循环三部分组成

1.菜单系统的使用
菜单结构
菜单采用树形结构设计一个父级菜单节点名下的所有孩子节点的parent全部指向该父亲节点同时孩子节点中有一个代表节点，
该代表节点的sibling指向另外一个同级孩子节点，菜单显示的排序取决于sibling链条
菜单创建
详情见init_menu_parents()函数
菜单功能挂载
需要在菜单中运行的函数定义在project/code/my_task_function.hpp文件中，定义后挂载在project/code/my_task_function.hpp相应的菜单回调函数下
随后将改回调函数挂载在project/code/my_task_function.hpp的menu_system函数中，菜单回调函数命名规范menu_mode_+ID，其中ID是创建菜单节点时节
点设置的ID，如下:
typedef struct Menu {
    const char *name;
    int id;
    void (*func)();
    struct Menu *parent;
    struct Menu *child;
    struct Menu *sibling;
} Menu;
ID 是菜单的身份证，同时只有叶子节点的菜单节点的回调函数才会生效，所以实现的功能必须挂载在叶子节点下
修改name可改变ips显示

线程函数
线程函数在project/code/my_global.hpp中定义，在初始化时将该函数指针丢到zf_driver_pit_rt的init对象中并设置周期，这类函数后台静默调用
总初始化函数已经封装到project/code/my_global.hpp中

主函数中运行菜单系统

其他
ips显示屏，tcp图传已基于逐飞大改
控制器参数由param_loading_from_file("/home/root/car_config.txt");从开发板本地文件中加载，目前实现写入，
当前速度环有两套方案，ladrc与传统PID，ladrc核心算法存在于project/code/LADRC.hpp，二级封装到project/code/my_pid.hpp
在/home/root/car_config.txt中可选择，初始化函数将依据此文件自动初始化相关线程，两种控制方案使用的pwm设置函数封装不同

图像处理是跪物


